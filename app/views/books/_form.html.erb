<%= form_with model:@book,local:true do |f| %>
  <% if @book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h2>
      <ul>
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :body %>
    <%= f.text_area :body %>
  </div>

　<div class="field">
    <%= f.label :genre %>
    <%= f.select :genre, Book.genres.keys.to_a, {} %>
　</div>


  <div>

    <labe><%= f.radio_button :genre,"1" %>ジャンル</labe>
      <%= f.collection_select :body, Book.all, :id ,:body %>
    </div>

  <div>
    <%= f.label :prodction %>
    <%= f.select :prodction, Book.prodctions.keys.to_a, {} %>
  </div>
  <div>
    <%= f.label :status %>
    <%= f.select :status, Book.statuses.keys.to_a, {} %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

rails g migration AddProfileImageIdToUsers profile_image_id:string
